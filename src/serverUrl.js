// This app works with a amazon lambda function which will reflect an emoji in the Rank Component.
// Do add the amazon lambda url in the Rank component.

// Add the backend server URL in the following line : 

const backendUrl = 'http://localhost:3000';

export default backendUrl;